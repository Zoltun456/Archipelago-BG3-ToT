# Config Files

`build_config.json`

Build metadata, sample YAML defaults, and local packaging settings.

`trials_unlock_catalog.json`

The Trials unlock pool used to build AP shop checks and item entries.
